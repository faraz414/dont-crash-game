# Don't Crash – Processing Game

A simple 2D game built using Processing (Java).

The objective of the game is to control the player and avoid crashing into obstacles for as long as possible.

## Features
- Player movement controls
- Obstacle spawning
- Collision detection
- Score tracking
- Game loop using `setup()` and `draw()`

## Technologies Used
- Java
- Processing

## How to Run
1. Open the project in Processing
2. Run the main `.pde` file
